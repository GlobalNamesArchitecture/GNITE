<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="description" content="GNITE is a web application that allows biologists and taxonomists to view, edit, and add data related to the classification of all known biological species.">
  <title>GNITE</title>
  <%= stylesheet_link_tag :all, :recursive => true %>
  <%= csrf_meta_tag %>
</head>
<body class="<%= body_class %>">
  <div id="wrapper">
    <% if current_user %>
      <%= render :partial => 'shared/navigation' %>
    <% end %>
    <div id="content" class="clearfix">
      <% if content_for?(:header) %>
        <div id="header">
          <h1><%= yield :header %></h1>
        </div>
      <% end %>

      <%= render :partial => 'shared/flashes' -%>
      <%= yield %>
    </div>
    <%= render :partial => 'shared/javascript' %>
  </div>
</body>
</html>
